#pragma once

const double MAX_AXIS_VAL = 85.0;

enum JoypadButton {
    DpadRight,
    DpadLeft,
    DpadDown,
    DpadUp,
    Start,
    ZButton,
    BButton,
    AButton,
    RightCButton,
    LeftCButton,
    DownCButton,
    UpCButton,
    RButton,
    LButton
};

enum JoypadAxis {
    XAxis,
    YAxis
};